# TODO

-   Add documentation
-   Add tests
-   Optimize the blogs so that they are not read from the file system every time a request is made. Consider using a caching mechanism or loading the blogs into memory at startup.
